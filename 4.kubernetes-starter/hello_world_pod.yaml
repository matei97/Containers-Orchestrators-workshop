apiVersion: v1
kind: Pod
metadata:
  name: helloworld
  labels:
    environment: development
spec:
  containers:
  - name: backend
    image: matei97/workshop:back-end-app
    ports:
    - containerPort: 8080
    resources:
      limits:
        memory: "64Mi"
        cpu: "500m"